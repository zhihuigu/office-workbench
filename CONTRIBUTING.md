# 参与维护

本项目使用 MIT 许可证，见 LICENSE。提交贡献时，请确认你有权以该许可证提供相关代码或文档；不得引入授权不兼容的材料。

保持项目轻量：CASE.md 是事实源；派生视图可重建；代码与私人目录分离。新增服务器、外部服务、后台进程或依赖前需明确用途、隐私和授权代价。

修改后运行 tests/run.py；测试输入必须在临时目录生成，不能提交原始测试样本、业务附件、日志或截图。新增可分发文件需逐项加入 PUBLIC_FILES.txt，并通过 tools/release.py check。检查通过不代替人工读文件。

提交说明应写问题、行为变化、如何验证和局限；不要把个人工作目录、机器路径或聊天历史带入代码、文档、提交消息或 Git 历史。除非实际运行验证，不新增平台支持声明。
